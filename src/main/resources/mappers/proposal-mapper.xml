<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwill.spring2.repository.ProposalRepository">

    <insert id="insert">
        INSERT INTO proposal(TITLE, CONTENT, PROPOSAL_TYPE, CREATED_TIME, MODIFIED_TIME)
        VALUES (#{title}, #{content}, #{proposal_type}, systimestamp, systimestamp)
    </insert>

</mapper>   