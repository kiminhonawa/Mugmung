<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwill.spring2.repository.CaptureRepository">

<!-- id의 값은 PostRepository 인터페이스에서 선언하는 메서드 이름. -->
    <insert id="insert">
        insert into CAPTURE (REPLY_TEXT,CAPTURE_IMAGES_ID ,WRITER,CREATED_TIME, MODIFIED_TIME)
        values (#{reply_text},#{capture_images_id} ,#{writer}, systimestamp, systimestamp)
    </insert>
</mapper>
